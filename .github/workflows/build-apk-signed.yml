name: Build Android APK (Debug)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: bun install

      - name: Build Web
        run: bun run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        continue-on-error: true
        with:
          tag_name: ${{ github.ref_name }}
          name: "Delta Stars ${{ github.ref_name }}"
          body: |
            ## Delta Stars ${{ github.ref_name }}
            
            ### Downloads:
            - **APK** (Debug - ready to install): `app-debug.apk`
            
            ### Installation:
            1. Download the APK file
            2. Open it on your Android device
            3. Tap "Install"
            4. If prompted, enable "Install from unknown sources"
            
            **Note:** This is a debug APK, ready to install without signing.
            
            **Owner:** Ali Aldahan (deltastars777@gmail.com)
          files: |
            android/app/build/outputs/apk/debug/*.apk
          draft: false
          prerelease: false

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: delta-stars-apk
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
