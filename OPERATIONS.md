# 🎛️ دليل تشغيل Delta Stars

## البوابات (Portals)

| البوابة | المسار | الوصول |
|---|---|---|
| المتجر الرئيسي | `/` | جميع الزوّار |
| لوحة الأدمن | `/admin` | مدير + بصمة |
| لوحة المطور | `/developer` | مطور فقط |
| لوحة السائق | `/driver` | السائقون |
| بوابة VIP/B2B | `/vip` | الشركات |
| تتبع الطلب | `/track/:id` | العميل |

## بيانات دخول افتراضية (غيّرها فوراً بعد أول دخول)

```
Admin:     admin@deltastars.sa / DeltaStars@2026!
Developer: dev@deltastars.sa   / Dev@Delta2026!
Driver:    driver001           / Driver@2026!
```

## الأقسام الفعّالة في لوحة التحكم

### 1. إدارة المنتجات
- إضافة/تعديل/حذف منتجات (237 منتج حقيقي مُحمَّل).
- تحديث الأسعار الجماعي.
- إدارة الفئات (خضروات، فواكه، أعشاب، ...).

### 2. إدارة الطلبات (Live Order Console)
- شاشة استقبال طلبات لحظية.
- تخصيص السائق تلقائياً (أقرب فرع).
- تحديث حالة الطلب: مقبول → قيد التحضير → في الطريق → تم التسليم.
- إلغاء/استرداد المبلغ.

### 3. إدارة السائقين والمناديب
- إضافة سائق: الاسم، الجوال، الفرع، رقم اللوحة.
- تفعيل/تعطيل الحساب.
- تتبع الموقع لحظياً (GPS).
- إحصائيات: عدد الطلبات، متوسط زمن التسليم، التقييم.

### 4. المحاسبة والفواتير
- فواتير إلكترونية متوافقة مع ZATCA.
- تقارير يومية/أسبوعية/شهرية/سنوية.
- تسوية آلية مع البنك الأهلي (Moyasar).

### 5. الحملات الترويجية
- إنشاء عرض: نسبة خصم، فترة، منتجات مستهدفة.
- كوبونات: توليد أكواد فريدة.
- إشعارات دفعية لجميع العملاء.

### 6. الإعلانات (Banners)
- بانرات الصفحة الرئيسية.
- إعلانات صفحة المنتج.
- تحديد الجمهور المستهدف.

### 7. بوابة VIP
- تسجيل عملاء الشركات (سجل تجاري + ضريبي).
- أسعار خاصة.
- فواتير آجلة (30/60/90 يوم).
- تفاوض إلكتروني.

### 8. تقارير التنبؤ الذكي
- توقع الطلب اليومي بناءً على البيانات التاريخية.
- تنبيهات المخزون (قبل النفاد).
- اقتراحات تسعير.

### 9. المساعد الذكي "عدي"
- 237 منتج مُحمَّل في القاعدة المحلية.
- إجابات فورية (<5ms) بدون إنترنت.
- يجيب عن: الأسعار، ساعات العمل، الفروع، الخدمات.

## الأمان
- 🔐 تشفير TLS 1.3
- 🔐 مصادقة بصمة/وجه (WebAuthn) لجميع البوابات
- 🔐 OTP عبر Authentica
- 🔐 مفاتيح API مشفّرة في `.env` (لا تظهر في الكود)
- 🔐 Argon2id لكلمات السر

## النسخ الاحتياطية
- **قاعدة البيانات**: Supabase تلقائي كل 24 ساعة.
- **الملفات**: Firebase Storage.
- **الاستعادة**: من لوحة المطور.

## المراقبة
- **Sentry**: تتبع الأخطاء.
- **Firebase Analytics**: سلوك المستخدمين.
- **Lighthouse CI**: أداء الويب (كل نشر).

## سير عمل الطلب

```
العميل يطلب
    ↓
تحقق OTP + دفع Moyasar
    ↓
النظام يبحث عن أقرب فرع (GPS)
    ↓
يعيّن أقرب سائق متاح
    ↓
السائق يستلم من الفرع
    ↓
تتبع لحظي على الخريطة
    ↓
تسليم + تأكيد بصمة/رمز
    ↓
فاتورة إلكترونية + تقييم
```

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| شاشة سوداء عند البدء | تأكد من وجود `splash_official_banner.jpg` في `public/` |
| المساعد لا يجيب | تحقق من `public/adi/products.json` |
| GPS لا يعمل | أذن الموقع من إعدادات الجهاز |
| الدفع يفشل | تأكد من `MOYASAR_SECRET_KEY` في `.env` الخادم |
| APK لا يُبنى | راجع GitHub Actions logs |

---
© 2026 Delta Stars — Ali Aldahan
